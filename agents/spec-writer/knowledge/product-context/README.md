# Product Context

Place your vision docs and planning priorities here as Markdown files. The spec-writer agent reads these before generating any spec to ground output in current product vision and priorities.

## Suggested files
- `vision.md` — Product vision and strategy
- `priorities.md` — Current planning priorities and focus areas
- Any other context docs the agent should reference

The agent will ask if these need updating before starting a new spec.
