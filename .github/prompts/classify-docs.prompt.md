# Classify Documents

Run the document classification script to sort today's edited documents into projects.

Execute this command now:

```powershell
node "$env:USERPROFILE\OneDrive - Microsoft\Automations\tools\classify-docs.js"
```

After running, report:
- How many documents were found
- Which projects they were classified to
- Any documents that need manual sorting
