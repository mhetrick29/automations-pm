# Ideas

This folder stores idea triage output from the `@idea-triage-agent`.

## Categories
- `apps/`
- `features/`
- `process/`

Files in the category folders are gitignored and local-only. They are pipeline artifacts produced by idea triage, not version-controlled project docs.

To triage a new idea, use `@idea-triage-agent`.
